import PageHeading from '../../components/PageHeading';

export default function PlatformSection() {
    return (
        <section className="platform-section">
            <PageHeading title={"Platform"} />
            <h2 className="platform-heading">Kinesis makes real-time Location Visibility easy and accessible</h2>
            <p className="platform-subheading">A sensor-to-API location visibility platform</p>
        </section>
    );
}